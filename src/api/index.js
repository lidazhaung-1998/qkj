import request from '@/utils/request'

const api = {}

export function getIndexData (params) {
  return request({
    url: '',
    method: 'GET',
    params
  })
}

export function addUser (data) {
  return request({
    url: '',
    method: 'POST',
    data
  })
}